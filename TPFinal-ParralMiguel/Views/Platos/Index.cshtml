@model IEnumerable<TPFinal_ParralMiguel.Models.Plato>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlatoNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlatoPrecio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlatoCantidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlatoNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlatoPrecio)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PlatoCantidad)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlatoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlatoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlatoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
